import React from 'react';
export interface ProgressProps {
    id: string;
    done: boolean;
    children: React.ReactNode;
    expiresIn?: number;
    progress: {
        startTime: number;
        endTime: number;
    };
}
export declare const Progress: ({ id, children, progress, done, expiresIn }: ProgressProps) => JSX.Element;
//# sourceMappingURL=index.d.ts.map